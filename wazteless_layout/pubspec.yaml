name: wazteless_layout
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.2
  intl: ^0.18.0
  latlong2: ^0.9.1
  flutter_map: ^8.1.1
  flutter_map_location_marker: ^10.0.0
  
  location: ^8.0.0
  flutter_polyline_points: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/models/articlesdata.json
    - lib/models/data.json
    - lib/models/wikirate.json
